import React from 'react';
import { Asset, AssetDocument } from '../types';
declare global {
    interface Window {
        cloudinary: any;
    }
}
declare type Props = {
    onSelect: (assets: Asset[]) => void;
    onClose: () => void;
    selectedAssets?: AssetDocument[];
    selectionType: 'single' | 'multiple';
};
declare type State = {
    loadingMessage: string | null;
    hasConfig: boolean;
};
export default class CloudinaryAssetSource extends React.Component<Props, State> {
    static defaultProps: {
        selectedAssets: undefined;
    };
    state: {
        loadingMessage: string;
        hasConfig: boolean;
    };
    private contentRef;
    private library;
    private domId;
    componentDidMount(): void;
    private setupMediaLibrary;
    handleSelect: ({ assets }: any) => void;
    handleClose: () => void;
    renderConfigWarning(): JSX.Element;
    render(): JSX.Element;
}
export {};
